const names = ['abul','babul','shabul','kabul','abul','babul'];
function removeduplicate(names){
    const unique =[];
    for (let i = 0; i < names.length; i++){
        const element = names[i];
        console.log()
    }
}